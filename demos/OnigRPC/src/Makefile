# AUTOMATICALLY GENERATED ON Sat May 17 2014 22:09:16 GMT+0100 (BST) #


SOURCES = OnigRPC.cpp Scanner.cpp onig-cache.cpp onig-reg-exp.cpp onig-result.cpp onig-scanner.cpp onig-searcher.cpp unicode-utils-posix.cpp unicode-utils-win.cpp
TARGET = Onig

OSNAME := $(shell python $(NACL_SDK_ROOT)/tools/getos.py)
CFLAGS = -O3

# onig nacl port
PROJECT_ROOT :=  $(abspath ../../../)
NACLPORTS_LOC := $(PROJECT_ROOT)/naclports

everything: $(NACL_SDK_ROOT)/toolchain/$(OSNAME)_pnacl/usr/include/oniguruma.h all

$(NACLPORTS_LOC)/make_all.sh:
	cd $(PROJECT_ROOT) &&  git submodule update --init

$(NACL_SDK_ROOT)/toolchain/$(OSNAME)_pnacl/usr/include/oniguruma.h: $(NACLPORTS_LOC)/make_all.sh
	cp -rf ../naclport_onig/onig $(NACLPORTS_LOC)/ports/
	cd $(NACLPORTS_LOC) && ./make_all.sh onig



include $(NACL_SDK_ROOT)/tools/common.mk

# Need to set SOURCES and TARGET
LIBS = $(DEPS) NativeCalls ppapi_cpp ppapi pthread onig

CFLAGS += -Wall

# Build rules generated by macros from common.mk:
ifeq ($(FORCE),y)
$(info Forcing make)
forceall:
	$(MAKE) -B FORCE=n
endif


$(foreach src,$(SOURCES),$(eval $(call COMPILE_RULE,$(src),$(CFLAGS))))

ifeq ($(CONFIG),Release)
$(eval $(call LINK_RULE,$(TARGET)_unstripped,$(SOURCES),$(LIBS),$(DEPS)))
$(eval $(call STRIP_RULE,$(TARGET),$(TARGET)_unstripped))
else
$(eval $(call LINK_RULE,$(TARGET),$(SOURCES),$(LIBS),$(DEPS)))
endif

$(eval $(call NMF_RULE,$(TARGET),))