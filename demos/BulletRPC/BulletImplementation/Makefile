# AUTOMATICALLY GENERATED ON Thu May 08 2014 21:38:16 GMT+0100 (BST) #

CONFIG ?= Debug
TOOLCHAIN ?= newlib
NACL_ARCH ?= x86_32

SOURCES = BulletRPC.cpp BulletInterface.cpp 
TARGET = Bullet
CFLAGS = -I../../../cpp -I$(NACL_SDK_ROOT)/toolchain/mac_pnacl/usr/include/bullet

include $(NACL_SDK_ROOT)/tools/common.mk

# Need to set SOURCES and TARGET
LIBS = $(DEPS) RPCRuntime JSONRPC RPCTransport RPCRequest RPCType ppapi_cpp ppapi pthread BulletSoftBody BulletDynamics BulletCollision LinearMath

CFLAGS += -Wall

# Build rules generated by macros from common.mk:
ifeq ($(FORCE),y)
$(info Forcing make)
forceall:
	$(MAKE) -B FORCE=n
endif


$(foreach src,$(SOURCES),$(eval $(call COMPILE_RULE,$(src),$(CFLAGS))))

ifeq ($(CONFIG),Release)
$(eval $(call LINK_RULE,$(TARGET)_unstripped,$(SOURCES),$(LIBS),$(DEPS)))
$(eval $(call STRIP_RULE,$(TARGET),$(TARGET)_unstripped))
else
$(eval $(call LINK_RULE,$(TARGET),$(SOURCES),$(LIBS),$(DEPS)))
endif

$(eval $(call NMF_RULE,$(TARGET),))

