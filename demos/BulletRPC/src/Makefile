# AUTOMATICALLY GENERATED ON Thu May 08 2014 21:38:16 GMT+0100 (BST) #

CONFIG ?= Debug
TOOLCHAIN ?= newlib
NACL_ARCH ?= x86_32

SOURCES = BulletRPC.cpp BulletInterface.cpp 
TARGET = Bullet
OSNAME := $(shell python $(NACL_SDK_ROOT)/tools/getos.py)
CFLAGS = -I$(NACL_SDK_ROOT)/toolchain/$(OSNAME)_pnacl/usr/include/bullet -O3

# bullet nacl port
PROJECT_ROOT :=  $(abspath ../../../)
NACLPORTS_LOC := $(PROJECT_ROOT)/naclports

everything: $(NACL_SDK_ROOT)/toolchain/$(OSNAME)_pnacl/usr/include/bullet/btBulletCollisionCommon.h all

$(NACLPORTS_LOC)/make_all.sh:
	cd $(PROJECT_ROOT) &&  git submodule update --init

$(NACL_SDK_ROOT)/toolchain/$(OSNAME)_pnacl/usr/include/bullet/btBulletCollisionCommon.h: $(NACLPORTS_LOC)/make_all.sh
	cd $(NACLPORTS_LOC) && ./make_all.sh bullet


include $(NACL_SDK_ROOT)/tools/common.mk

# Need to set SOURCES and TARGET
LIBS = $(DEPS) NativeCalls ppapi_cpp ppapi pthread BulletSoftBody BulletDynamics BulletCollision LinearMath


# Build rules generated by macros from common.mk:
ifeq ($(FORCE),y)
$(info Forcing make)
forceall:
	$(MAKE) -B FORCE=n
endif


$(foreach src,$(SOURCES),$(eval $(call COMPILE_RULE,$(src),$(CFLAGS))))

ifeq ($(CONFIG),Release)
$(eval $(call LINK_RULE,$(TARGET)_unstripped,$(SOURCES),$(LIBS),$(DEPS)))
$(eval $(call STRIP_RULE,$(TARGET),$(TARGET)_unstripped))
else
$(eval $(call LINK_RULE,$(TARGET),$(SOURCES),$(LIBS),$(DEPS)))
endif

$(eval $(call NMF_RULE,$(TARGET),))

